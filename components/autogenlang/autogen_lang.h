#ifndef _AUTOGEN_LANG_H
#define _AUTOGEN_LANG_H

// Minimal stub generated header for locale support
// Only English is defined, enough for basic UI usage.

#define BUCKETS_NUM 16
#define LOCALE_NUM_LANGUAGES 1

typedef enum locale {
    LOCALE_EN = 0,
} jlocale_t;

#endif //_AUTOGEN_LANG_H

